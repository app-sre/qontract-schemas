---
"$schema": /metaschema-1.json
version: '1.0'
type: object

additionalProperties: false
properties:
  "$schema":
    type: string
    enum:
    - /openshift/resource-template-1.yml
  provider:
    type: string
    enum:
    - resource-template
  path:
    "$ref": "/common-1.json#/definitions/resourceref"
  type:
    type: string
    enum:
    - jinja2
    - extracurlyjinja2
  variables:
    type: object
  validate_alertmanager_config:
    type: boolean
  enable_query_support:
    type: boolean
    description: indicates that this resource is using graphql query results
required:
- path
